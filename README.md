# Autonomous Self-Correcting ReAct Agent

An autonomous AI Agent built on the ReAct (Reason + Act) architecture featuring explicit state tracking, safe tool execution, runtime failure evaluation, and dynamic self-correction loops.

---

## 🏗️ System Architecture

The agent follows an iterative control loop designed to handle tool failures, parsing errors, and goal drift: